[package]
name = "dds-detection"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
socket2 = "0.5"
serde = { version = "1.0", features = ["derive"] }
toml = "0.7"  # For config file parsing
log = "0.4"
env_logger = "0.10"
